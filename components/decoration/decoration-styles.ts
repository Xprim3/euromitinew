/**
 * Shared tokens for marketing decorations — aligns with DESIGN.md brand vars (#0f172a, #f59e0b, #b91c1c).
 * Use via Tailwind arbitrary values or reuse in SVG stroke/fill utilities.
 */
export const decorationTokens = {
  navy: "#0F172A",
  gold: "#F59E0B",
  red: "#B91C1C",
} as const
